dashboard:
- allow user to sort by "recently viewed" instead of the current "recently ran"
- allow filtering by algorithm and env name
- add hparam diff bw runs
missing from algs:
- min over critics
- update ent coef
